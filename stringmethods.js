let test= "bharat sahane";
console.log(test.length);

let text1 ="i like mango";
console.log(text1.silce(1,6)); // start indcluding end ind: exclusive

console.log (text1.slice(1));

console.log(text1.slice(-6,-1)); //-6 is exclusive , -1 ind:exclusive

console.log(test1.substring(-5,5)); //neg index will treated as 0 here
console.log(text1.substr(4,6)); // end index will consider here as a length

// the replace () method does not change the string it is called on.
//the replace() method returns a new string.
// the replace () method replacec only the first match.
// if you want to replace all matches use a regular expression with the/ g flag set sec


Var details = "mumbai is metrocity. mumbai i like mumbai";
